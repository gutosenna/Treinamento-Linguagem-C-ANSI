#include <stdio.h>
#include <stdlib.h>

// Inicializando Ponteiros

char *p; // Há um erro semântico pois o ponteiro não foi inicializado

int main(int argc, char *argv[]) {
	
	printf(p);
	return 0;
}
